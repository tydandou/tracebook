# Index Maintenance Rules

When creating project knowledge, update its project `index.md`. Update
`project-status.md` when current state changes. Update a source map whenever a
new code-path relationship is recorded. Update the appropriate domain or
pattern entry when writing cross-project knowledge.

Treat a document as an orphan during a health check when no entry index, source
map, project status, or parent document references it.
