interface:
  display_name: "Tracebook"
  short_description: "Traceable knowledge for repository work"
  default_prompt: "Use $tracebook to load minimal project context before this repository task and capture only verified durable conclusions afterward."
policy:
  allow_implicit_invocation: true
